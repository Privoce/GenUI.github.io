# 组件类型

这里的组件类型仅仅指内置组件和自定义组件

## 内置组件

内置组件指的是在开发时直接可以使用的，无需自己开发的底层组件，这些底层组件由[GenUI Builtin Components](/zh/components/index)提供。

所有内置组件的属性和回调都是不可更改的并且带有内置类型

## 自定义组件

自定义组件指的是在开发时基于已有的组件进行二次开发的组件。

讲的简单一点，只要是使用`<component name="SpecialName"></component>`包裹起来的组件都是自定义组件。

1. 自定义组件的`name`表示组件被外部引用时的组件名称
2. 自定义组件也可以有`id`属性，`id`和`name`互相独立，对自定义组件来说`id`仅对内起效，`name`对外起效。即：[内外原则](/zh/doc/tutorial/template/basic#内外原则)
3. `<component>`标签没有任何含义，只代表对自定义组件的声明
4. 自定义组件默认继承`view`组件，所以`view`组件的属性自定义组件也可以使用
5. 自定义组件不继承`view`组件的回调只继承属性
6. `<component>`标签只能在根部使用